# PR-037 Stabilization Plan Update

## Status

```text
PR-037 go test ./... stabilization: static stabilization complete; real test run pending
```

This file is an incremental update to `docs/aicloud-implementation-plan.md`.

The main implementation plan is intentionally not overwritten in this step because previous tool responses truncated the long file content. This avoids accidentally dropping existing sections.

## Completed Static Fixes

### 1. Cluster API mapper

Updated files:

```text
infra/mapping/clusterapi/types.go
infra/mapping/clusterapi/mapper.go
infra/mapping/clusterapi/mapper_test.go
```

Fixed issues:

```text
- Replaced cluster.Metadata.* with cluster.Name / cluster.Namespace.
- Changed DesiredMachineDeployment.Replicas from int to int32.
- Updated tests to call NewManagedCluster(name, namespace, environment).
```

### 2. KubeVirt mapper

Updated files:

```text
infra/mapping/kubevirt/types.go
infra/mapping/kubevirt/mapper.go
infra/mapping/kubevirt/mapper_test.go
```

Fixed issues:

```text
- Replaced cluster.Metadata.* with cluster.Name / cluster.Namespace.
- Replaced class.Metadata.Name with class.Name.
- Removed StorageProfile because api.MachineClassSpec has no Storage field.
- Replaced string GPU assumptions with *api.GPUSpec handling.
- Changed ordinal type to int32 to match WorkerGroupSpec.Replicas.
```

### 3. Metal3 mapper

Updated files:

```text
infra/mapping/metal3/types.go
infra/mapping/metal3/mapper.go
infra/mapping/metal3/mapper_test.go
```

Fixed issues:

```text
- Replaced cluster.Metadata.* with cluster.Name / cluster.Namespace.
- Replaced class.Metadata.Name with class.Name.
- Removed StorageProfile because api.MachineClassSpec has no Storage field.
- Replaced string GPU assumptions with *api.GPUSpec handling.
- Changed ordinal type to int32 to match WorkerGroupSpec.Replicas.
```

### 4. yamlio

Updated files:

```text
integrations/gitops/yamlio/managedcluster.go
integrations/gitops/yamlio/managedcluster_test.go
integrations/gitops/yamlio/README.md
docs/yaml-parser-writer-dependency-decision.md
```

Fixed issues:

```text
- Replaced cluster.Metadata.* with embedded ObjectMeta fields.
- Updated NewManagedCluster call to include environment.
- Changed WorkerGroupYAML.Replicas to int32.
- Deferred gopkg.in/yaml.v3 until go.sum can be generated by Go tooling.
- Restored go.mod to no external dependencies.
- Replaced yamlio implementation with a narrow dependency-free ManagedCluster parser/formatter skeleton.
- Made label output deterministic by sorting label keys.
- Added deterministic label ordering test.
```

Boundary:

```text
The dependency-free yamlio implementation is not a general YAML parser.
It only supports the current ManagedCluster example shape required by the dry-run skeleton.
```

### 5. GitOps patch plan tests

Updated file:

```text
integrations/gitops/patch_plan_test.go
```

Fixed issue:

```text
- Renamed local variable proposal to changeProposal to avoid shadowing the imported proposal package.
```

### 6. GitOps helper inspection

Inspected files:

```text
integrations/gitops/manifest_writer_test.go
integrations/gitops/branch_plan_test.go
integrations/gitops/managedcluster_patch_test.go
```

Static result:

```text
- validManagedCluster(replicas int32) uses infraapi.NewManagedCluster(name, namespace, environment).
- WorkerGroupSpec.Replicas is int32.
- validPatchPlan uses infraapi.KindManagedCluster.
- No old cluster.Metadata usage was found in these helpers.
```

### 7. CI workflow

Updated file:

```text
.github/workflows/go-test.yml
```

Fixed issue:

```text
- Added go mod tidy before go test ./....
- Added git diff --exit-code -- go.mod go.sum to detect uncommitted module metadata changes.
```

## Verified By Static Inspection

Static checks and targeted file inspection did not identify remaining known API-shape mismatches for:

```text
Spec.Storage
cluster.Metadata
class.Metadata
Metadata.Name helper usage
NewManagedCluster missing environment argument
WorkerGroupSpec.Replicas int/int32 mismatch in inspected helpers
```

This is not equivalent to a successful compile.

## Still Not Verified

```text
go mod tidy
go test ./...
CI status
```

Current dependency state:

```text
go.mod currently has no external dependencies.
go.sum is not present and is not currently expected while go.mod has no external dependencies.
```

## Next Required Local/CI Commands

```bash
go mod tidy
go test ./...
git status --short
```

If a future dependency is reintroduced and `go.sum` is generated, commit it.

Do not hand-write go.sum. It should be generated by Go tooling.

## Next PR After Stabilization

```text
PR-038 wire yamlio into DryRunManifestWriter
```

Only start PR-038 after `go test ./...` status is known.
